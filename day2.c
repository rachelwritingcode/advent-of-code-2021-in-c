#include <stdio.h>
#include <stdlib.h>
#define MAX 2000

int main() {
    // Open input file
    FILE *file = fopen("day2_test.txt", "r");
    int depths[MAX];

    if (file != NULL) {
        
    } 
    return 0;
}

